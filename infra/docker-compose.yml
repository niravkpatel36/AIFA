version: "3.9"
services:
  api:
    build: ../backend
    env_file: .env
    ports:
      - "8000:8000"
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: secure
  redis:
    image: redis:7
